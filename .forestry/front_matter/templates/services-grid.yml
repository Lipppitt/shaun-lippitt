---
label: Services Grid
hide_body: true
fields:
- name: services
  type: list
  config:
    use_select: true
    min: 
    max: 
    source:
      type: pages
      section: services
  label: Services
